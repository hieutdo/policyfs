# `pfs move`

Moves files between storage targets based on the mount's mover configuration.

## Usage

```bash
pfs move <mount>
```

## systemd

- `pfs-move@<mount>.service`
- `pfs-move@<mount>.timer`
